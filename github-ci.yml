workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "push"

# include:
#   - local: "".github/workflows/merge_request_event.yml"
#     rules:
#       - if: $CI_PIPELINE_SOURCE == "merge_request_event"
#   - local: "".github/workflows/push.yml"
#     rules:
#       - if: $CI_PIPELINE_SOURCE == "push"
